{
  "name": "smartstudy-ai",
  "version": "1.0.0",
  "private": true,
  "description": "SmartStudy-AI with local rule analysis and OpenAI-backed advanced mode.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check app.js && node --check spotify-player.js && node --check finance-knowledge.js && node --check server.js",
    "smoke": "node ui-smoke.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "openai": "^4.104.0",
    "playwright": "^1.60.0"
  }
}
